version: '3.8'

services:
  web-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: forum-web
    ports:
      - 127.0.0.1:3000:3000
    environment:
      - NODE_ENV=prod

      